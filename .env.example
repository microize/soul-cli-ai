# Soul CLI Environment Configuration
# Copy this file to .env and fill in your API keys

# Note: OAuth credentials for Google authentication are built into the CLI
# as per Google's "installed application" OAuth flow design.
# Users authenticate through their browser - no OAuth setup needed.

# Other environment variables
GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_CLOUD_PROJECT=your_gcp_project_here